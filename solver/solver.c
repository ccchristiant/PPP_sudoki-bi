/*
** EPITECH PROJECT, 2023
** solver
** File description:
** solver
*/

#include <stdio.h>
#include <stdlib.h>
#include <sys/stat.h>
#include "solver.h"

int main(int ac, char **av)
{
    if (check_error(ac, av) == EXIT_ERROR)
        return (EXIT_ERROR);
    int grid[N][N];

    return (0);
}
